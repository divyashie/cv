/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Add this line to enable static export
    basePath: '/cv', // replace with your repository name
    assetPrefix: '/cv/', // replace with your repository name
  };
  
  export default nextConfig;